import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com','saint313.com','via.placeholder.com','images.pexels.com'],
  },
};

export default nextConfig;
